# 🎫 Day 17 – Bearer Token

## What is a Bearer Token?

A Bearer Token is a credential sent with an API request to prove that the caller has access to protected resources.

It is commonly sent through the HTTP Authorization header.

---

## Example

```http
Authorization: Bearer eyJhbGciOi...
